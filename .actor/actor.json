{
  "actorSpecification": 1,
  "name": "clientscout-ai",
  "title": "ClientScout AI",
  "description": "Discover the right clients. Anywhere. AI-powered multi-sector business lead generator using Google Maps.",
  "version": "1.0.0",

  "output": {
    "actorOutputSchemaVersion": 1,
    "title": "ClientScout AI – Business Leads",
    "description": "Structured list of business leads discovered using AI-powered Google Maps search.",
    "properties": {
      "name": {
        "title": "Business Name",
        "type": "string",
        "template": "{{value}}"
      },
      "phone": {
        "title": "Phone Number",
        "type": "string",
        "template": "{{value}}"
      },
      "website": {
        "title": "Website",
        "type": "string",
        "template": "{{value}}"
      },
      "address": {
        "title": "Address",
        "type": "string",
        "template": "{{value}}"
      },
      "rating": {
        "title": "Rating",
        "type": "number",
        "template": "{{value}}"
      },
      "reviewCount": {
        "title": "Review Count",
        "type": "number",
        "template": "{{value}}"
      },
      "category": {
        "title": "Category",
        "type": "string",
        "template": "{{value}}"
      },
      "googleMapsUrl": {
        "title": "Google Maps URL",
        "type": "string",
        "template": "{{value}}"
      },
      "searchQuery": {
        "title": "Search Query",
        "type": "string",
        "template": "{{value}}"
      }
    }
  }
}
